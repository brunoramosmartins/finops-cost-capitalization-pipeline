# Sample Data

This directory is intended for lightweight sample outputs generated by the
synthetic billing pipeline.

Expected contents after running Phase 1 tooling:

- `cloud_cost_usage_sample.csv`
- `cloud_cost_usage_sample_manifest.json`

These files are useful for documentation, lightweight review, and CI fixtures.
